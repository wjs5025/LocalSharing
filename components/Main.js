import React from 'react';
import { Platform, StyleSheet, View, Text, Button } from 'react-native';

function Main(){
    return(
        <View>
            <Text>메인페이지 입니다.</Text>
        </View>
    );
}
export default Main;